apiVersion: v1
kind: Service
metadata:
  name: {{ include "moeys-api.fullname" . }}
  labels:
    {{- include "moeys-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "moeys-api.selectorLabels" . | nindent 4 }}
