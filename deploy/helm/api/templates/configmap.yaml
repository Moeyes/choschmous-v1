apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moeys-api.fullname" . }}-config
  labels:
    {{- include "moeys-api.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
