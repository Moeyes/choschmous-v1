apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moeys-workers.fullname" . }}-config
  labels:
    {{- include "moeys-workers.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
