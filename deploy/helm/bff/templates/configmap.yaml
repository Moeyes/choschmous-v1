apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "moeys-bff.fullname" . }}-config
  labels:
    {{- include "moeys-bff.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
