apiVersion: v1
kind: Service
metadata:
  name: {{ include "moeys-bff.fullname" . }}
  labels:
    {{- include "moeys-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "moeys-bff.selectorLabels" . | nindent 4 }}
