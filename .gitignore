# Demo credentials — never commit
final/_rebuild/reskin/demo/CREDENTIALS.md

# Database backups
*.dump
*.sql.gz

# Local env / secrets
.env
.env.local
.env.production
.env.*
!.env.example
# UAT env file — contains DB/Redis credentials, must never be committed (CHOS-101)
uat.env

# OS / editor
.DS_Store
*.swp

# Claude Code local config / memory
.claude/

# Python tool caches (not covered by backend/.gitignore)
.pytest_cache/
.ruff_cache/
.mypy_cache/

# pnpm local content-addressable store (multi-hundred-MB cache, never commit)
.pnpm-store/
