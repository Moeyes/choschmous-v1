# .gitignore - common Python + FastAPI ignores
__pycache__/
*.pyc
.env
venv
.venv
.ipynb_checkpoints/
.github
concepts/
frontend/
package-lock.json
*.__pycache__/
makefile

# Local-only bootstrap utility — must never carry plaintext credentials in the repo
create_superadmin.py

# CHOS-202: locally-stored report artifacts (used when no S3 bucket is configured)
var/
# CHOS-503: test tooling artifacts (mutation/coverage/hypothesis)
mutants/
.mutmut-cache
mutmut-stats.json
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/
